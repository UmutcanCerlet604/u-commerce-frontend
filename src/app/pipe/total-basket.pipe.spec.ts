import { TotalBasketPipe } from './total-basket.pipe';

describe('TotalBasketPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalBasketPipe();
    expect(pipe).toBeTruthy();
  });
});
